#include <stdio.h>

/**
 * main - Print alphabet in lowercase, save q and e.
 *
 * Return: Always 0.
 */
int main(void)
{int num;
	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
